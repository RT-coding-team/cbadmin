extends ../../../layout/form
include ../../../mixins/button-save

block variables
    - var id = 'hostname'

block icon
    include ../../../icons/edit

block title
    | Hostname

block form
    div.position-relative
        input.w-100.input-text#hostname-input(placeholder='Hostname', name='hostname')
        +button-save(id)
